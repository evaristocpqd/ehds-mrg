{
  "name": "anchor-only-poc",
  "version": "1.0.0",
  "description": "EHDS-MRG anchor-only reproducibility repository",
  "private": false,
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:besu": "hardhat run scripts/deploy.js --network besu",
    "estimate:gas": "hardhat run scripts/estimate-gas.js --network besu",
    "workload": "node workload/generate-workload.js --rate 2000 --duration 3600 --out data/raw/generated-workload.csv"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  }
}
